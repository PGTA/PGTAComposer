
#pragma once

#include <cstdint>

namespace PGTAConstants
{
    const static uint16_t UUID_NUM_BYTES = 32;

    const static float MIN_GAIN = -95.0f;
    const static float MAX_GAIN = 6.0f;

    const static float MIN_PROBABILITY = 0.0f;
    const static float MAX_PROBABILITY = 1.0f;

    const static float MIN_START_TIME = 0.0f;
    const static float MIN_PERIOD = 0.0f;
};
